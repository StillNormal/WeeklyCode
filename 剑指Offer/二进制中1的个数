// 输入一个整数，输出该数二进制表示中1的个数。其中负数用补码表示。

class Solution {
public:
     int NumberOf1(int n) { // 关键 负数
         int cnt = 0;
         while (n != 0) {
             cnt++;
             n = n & (n-1); // 极其巧妙 每次消耗掉一个1
         }
         return cnt;
     }
};