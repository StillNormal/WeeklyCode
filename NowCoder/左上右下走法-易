#include <iostream>
using namespace std;

int ways(int x, int y) {
    if (x == 0 || y == 0) // 关键是递归 还有边界条件是到某个点算一种走法
        return 1;
    
    return ways(x, y-1) + ways(x-1, y);
}

int main() {
    int x, y;
    cin >> x >> y;
    cout << ways(x, y);
    return 0;
}